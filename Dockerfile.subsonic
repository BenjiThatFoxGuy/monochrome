FROM oven/bun:1.3.10-alpine

WORKDIR /app

COPY server/ ./server/

EXPOSE 4533

HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost:4533/health || exit 1

CMD ["bun", "server/index.js"]
